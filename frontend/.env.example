# =============================================================================
# API
# =============================================================================
# Base URL of the backend API.
# In development, leave empty — Vite proxy handles /api and /uploads automatically.
# In production, set to the full backend URL (e.g. https://api.yourdomain.com).
VITE_API_URL=
